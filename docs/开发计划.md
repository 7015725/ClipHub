# ClipHub 开发计划

## 阶段 0：项目骨架

目标：建立能够持续迭代的独立项目。

- [x] 创建 GitHub 仓库 `7015725/ClipHub`
- [x] 初始化 `main`
- [x] 添加 README 和设计文档
- [ ] 确定许可证
- [x] 建立唯一 ShortX 入口 `ClipHub.js`
- [x] 建立模块加载器
- [x] 建立远程 `module-manifest.json`
- [x] 入口自动下载 15 个模块
- [x] Git blob SHA 完整性校验
- [x] 临时目录下载、旧模块备份和失败恢复
- [x] 完整本地缓存的离线回退
- [x] 建立日志目录和数据库目录
- [x] 增加重复启动保护
- [x] 增加 Rhino ES5 静态检查
- [x] 完成运行目录、作用域和文件锁真机探测
- [x] 完成入口首次下载与模块 SHA 真机验证
- [x] 完成真实模块停止、清理和二次启动验证

完成标准：

- 用户只需在 ShortX 中执行完整入口；
- 首次运行由入口下载全部模块；
- 模块未变化时不重复下载；
- 下载失败不会破坏上一套模块；
- 可加载全部模块；
- 重复启动不会创建多实例；
- 停止入口可以完整清理资源。

当前状态：阶段 0 已完成真机验证。

## 阶段 1：剪贴板数据链路

目标：稳定保存文本剪贴板历史。

### 阶段 1A：SQLite 与 Repository 基础层

- [x] SQLite schema v1
- [x] 数据库迁移框架
- [x] 显式事务封装
- [x] 参数绑定执行接口
- [x] Cursor 查询与关闭边界
- [x] 剪贴板记录基础 CRUD
- [x] 软删除与恢复
- [x] 标签创建与记录关联基础接口
- [x] SQLite 真机探测 003
- [x] 入口首次下载与模块 SHA 真机验证
- [x] 下载后真实模块加载与 Repository 真机探测 004
- [x] schema v1 → v2 迁移
- [x] `is_sensitive` 字段与索引

当前状态：阶段 1A 已完成。

### 阶段 1B：ClipboardManager 链路

- [x] ClipboardManager 监听
- [x] ClipData 文本读取
- [x] 主动写入接口
- [x] 自身写入回环抑制
- [x] 短时间重复合并
- [x] 同一 clip 重复回调抑制
- [x] 事件序号和回调线程记录
- [x] 日志正文脱敏
- [x] 剪贴板业务探测 005
- [x] 跨 ShortX 任务停止控制实测
- [x] 连续复制 100 条压力测试
- [x] 多 item、空白、非文本和超长 ClipData 边界测试
- [x] 来源应用探测
- [x] 来源包名、应用名、UID 与置信度入库
- [x] 忽略应用运行期策略
- [x] 敏感 ClipData 默认跳过与允许保存策略

完成标准：

- 连续复制 100 条文本无崩溃且记录数准确；
- 重启后数据存在；
- 主动复制不会产生无限重复；
- 非文本剪贴板不会破坏监听器；
- 跨任务停止后监听器、数据库和文件锁均释放；
- 测试和更新期间不会形成双实例；
- 来源与敏感标记可可靠读取和恢复。

当前状态：005 v2、006 v2、007 和 008 已全部通过。阶段 1B 已完成。

### 阶段 1C：设置持久化与历史保留

- [x] 复用 SQLite `settings` 表
- [x] 设置值 JSON 序列化和反序列化
- [x] 类型、范围与枚举校验
- [x] `ignorePackages` 去空和去重
- [x] 敏感策略、来源开关和忽略应用即时应用
- [x] 历史数量裁剪实现
- [x] 保存天数清理实现
- [x] 置顶记录保护
- [x] 清理事务与结果统计
- [x] 自动清理安全默认值：`0/0` 禁用
- [ ] 设置重启持久化真机验证 009
- [ ] 数量、天数和置顶保护真机验证 009

完成标准：

- 设置写入后重启仍存在；
- 运行期策略立即同步到 Clipboard 模块；
- 默认升级不会删除正式历史；
- 非置顶历史可按天数和数量清理；
- 置顶记录不被自动清理；
- 第二次启动不会重复删除。

当前状态：模块集 `20260721.9` 已发布，等待探测 009。

## 阶段 2：基础悬浮窗

目标：完成可操作的最小 UI。

- [ ] WindowManager 单实例
- [ ] 标题栏
- [ ] 窗口拖动
- [ ] 安全区域限制
- [ ] 收起、展开和关闭
- [ ] 位置持久化
- [ ] 列表展示
- [ ] 单击复制
- [ ] 空状态
- [ ] 亮暗色适配

完成标准：

- 显示、拖动、收起和关闭正常；
- 不残留透明遮罩；
- 卡片点击可以复制原文；
- 屏幕尺寸变化后窗口仍可见。

## 阶段 3：管理能力

目标：形成完整的剪贴板管理器。

- [ ] 新增
- [ ] 编辑
- [ ] 删除与撤销
- [ ] 项目置顶
- [ ] 拖动排序
- [ ] 关键词搜索
- [ ] 来源应用过滤
- [ ] 内容类型过滤
- [ ] 自定义标签
- [ ] 标签过滤
- [ ] 长文本详情

完成标准：

- 搜索和组合过滤正确；
- 窗口拖动与项目排序不冲突；
- 标签删除不删除原记录；
- 编辑和删除后 UI 与数据库一致。

## 阶段 4：内容识别与快捷操作

目标：提高剪贴板内容的可操作性。

- [ ] URL 识别
- [ ] 电话识别
- [ ] URL 打开
- [ ] 电话拨号
- [ ] 内容类型手动修正
- [ ] 分类识别单元测试样例
- [ ] 误判保护

完成标准：

- 识别不修改原始文本；
- 打开和拨号交由系统界面；
- 订单号等长数字不会被轻易识别为电话。

## 阶段 5：翻译与高级设置

目标：增加文本处理能力和设置页 UI。

- [ ] 翻译引擎接口
- [ ] 翻译配置
- [ ] 翻译结果复制
- [ ] 翻译结果另存
- [ ] 历史数量设置 UI
- [ ] 自动清理设置 UI
- [ ] 忽略应用列表 UI
- [ ] 敏感内容策略 UI
- [ ] 导入导出
- [ ] 数据库备份恢复

## 阶段 6：稳定性与发布

- [ ] manifest 自动生成
- [ ] 模块更新并发锁
- [ ] 入口运行中更新生命周期
- [ ] 独立签名验证
- [ ] Stable/Beta 通道
- [ ] 入口文件更新提示
- [ ] 异常恢复审查
- [ ] 主线程与 WindowManager 审查
- [ ] 数据库事务审查
- [ ] 内存与监听器泄漏审查
- [ ] 平板布局验证
- [ ] Android 13 / 14 / 15 兼容验证
- [ ] 用户日志脱敏验证
- [ ] README 使用说明
- [ ] 版本更新说明
- [ ] 首个 Beta 发布

## 推荐开发顺序

```text
入口下载与模块生命周期
→ 数据库
→ 剪贴板监听
→ 去重与回环抑制
→ 压力与异常 ClipData
→ 来源应用与隐私策略
→ 设置持久化与历史清理
→ 最小悬浮列表
→ 复制与删除
→ 搜索过滤
→ 标签
→ 编辑与排序
→ URL/电话识别
→ 翻译
→ 高级设置 UI
```

## 真机探测顺序

```text
001 运行目录 / eval / 单任务文件锁
→ 002 双任务并发文件锁
→ 003 Android 原生 SQLite 基础能力
→ ShortX 执行入口并下载 15 个模块
→ 004 本地 manifest / 模块 SHA / schema / Repository CRUD
→ 005 ClipboardManager / 回环抑制 / 重复合并
→ 006 跨任务停止控制 / 100 条压力 / 异常 ClipData
→ 007 来源应用与敏感 ClipData 能力
→ 008 来源持久化 / schema v2 / 敏感与忽略策略
→ 009 设置持久化 / 历史数量与天数清理
→ WindowManager / Looper / Handler 生命周期
```

探测结果保存到：

```text
shortx.getShortXDir()/ClipHub/probes/
```
