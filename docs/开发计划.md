# ClipHub 开发计划

## 阶段 0：项目骨架

目标：建立能够持续迭代的独立项目。

- [x] 创建 GitHub 仓库 `7015725/ClipHub`
- [x] 初始化 `main`
- [x] 添加 README 和设计文档
- [ ] 确定许可证
- [x] 建立唯一 ShortX 入口 `ClipHub.js`
- [x] 建立模块加载器
- [x] 建立远程 `module-manifest.json`
- [x] 入口自动下载 15 个模块
- [x] Git blob SHA 完整性校验
- [x] 临时目录下载、旧模块备份和失败恢复
- [x] 完整本地缓存的离线回退
- [x] 建立日志目录和数据库目录
- [x] 增加重复启动保护
- [x] 增加 Rhino ES5 静态检查
- [x] 完成运行目录、作用域和文件锁真机探测
- [x] 完成入口首次下载与模块 SHA 真机验证
- [x] 完成真实模块停止、清理和二次启动验证

当前状态：阶段 0 已完成真机验证。

## 阶段 1：剪贴板数据链路

### 阶段 1A：SQLite 与 Repository 基础层

- [x] SQLite schema v1
- [x] 数据库迁移框架
- [x] 显式事务封装
- [x] 参数绑定执行接口
- [x] Cursor 查询与关闭边界
- [x] 剪贴板记录基础 CRUD
- [x] 软删除与恢复
- [x] 标签创建与记录关联基础接口
- [x] schema v1 → v2 迁移
- [x] `is_sensitive` 字段与索引

当前状态：阶段 1A 已完成。

### 阶段 1B：ClipboardManager 链路

- [x] ClipboardManager 监听
- [x] ClipData 文本读取
- [x] 主动写入接口
- [x] 自身写入回环抑制
- [x] 短时间重复合并
- [x] 同一 clip 重复回调抑制
- [x] 来源包名、应用名、UID 与置信度入库
- [x] 忽略应用运行期策略
- [x] 敏感 ClipData 默认跳过与允许保存策略
- [x] 连续复制压力与边界验证

当前状态：005 v2、006 v2、007 和 008 已全部通过。阶段 1B 已完成。

### 阶段 1C：设置持久化与历史保留

- [x] 复用 SQLite `settings` 表
- [x] 设置值 JSON 序列化和反序列化
- [x] 类型、范围与枚举校验
- [x] 历史数量裁剪
- [x] 保存天数清理
- [x] 置顶记录保护
- [x] 设置与清理真机验证 009

当前状态：阶段 1C 已完成。

## 阶段 2：基础悬浮窗

### 阶段 2A：WindowManager 生命周期

- [x] `TYPE_APPLICATION_OVERLAY`
- [x] add/update/remove 主线程同步
- [x] 标题栏拖动
- [x] WindowMetrics 与系统栏安全区域
- [x] 四边位置夹紧
- [x] 折叠、展开和固定状态
- [x] 亮暗色基础样式
- [x] 010 v2 与 011 真机验证

当前状态：阶段 2A 已完成。

### 阶段 2B：位置与尺寸生命周期

- [x] 窗口位置 SQLite 持久化
- [x] 归一化位置比例恢复
- [x] 屏幕旋转和尺寸变化监听
- [x] 安全区域变化后重新夹紧
- [x] 竖屏到横屏真机验证 012
- [ ] 平板和多窗口尺寸变化验证

当前状态：手机竖横屏范围已完成。

### 阶段 2C：最小历史列表

- [x] 原生 ScrollView 卡片列表
- [x] 最近记录查询与展示
- [x] Clipboard EventBus 自动刷新
- [x] 单击复制
- [x] 空状态
- [x] 敏感记录正文遮罩
- [x] 最小列表真机验证 013

当前状态：功能链路已完成，视觉审查并入阶段 3D。

### 阶段 2D：系统手势与 UI 退出（必要功能）

本阶段属于首版发布阻断条件。未通过专项真机验证前，不进入正式发布收口。

- [x] 接入 Android 系统返回回调；
- [x] Android 13 及以上兼容 `OnBackInvokedDispatcher`；
- [x] Android 14 支持预测性返回进度、取消和提交；
- [x] 增加三键返回键和 Escape 返回后备链；
- [x] 次级页面返回上一层；
- [x] 首页返回关闭全部 ClipHub UI；
- [x] 页面显示后统一注册系统返回分发；
- [x] 增加进入最近任务或切换后台后的 UI 隐藏判断；
- [x] 后台隐藏时关闭筛选、编辑、详情、标签和主列表窗口；
- [x] UI 关闭后继续保持剪贴板监听、SQLite、运行锁和控制入口；
- [x] 再次打开时从首页开始；
- [x] 编辑未保存内容不会因进入后台自动提交；
- [x] 所有退出入口复用同一套幂等 UI 清理链；
- [x] 系统回调采用全局签名去重，程序化返回按页面所有者去重；
- [x] 关闭链清理残留 WindowManager View 和返回回调引用；
- [x] 增加系统手势专项探测 030；
- [x] 探测 030 v2 暴露全局 180ms 去重误吞编辑页和首页返回；
- [x] 模块集 `20260722.24` 修正不同页面连续返回和残留回调清理；
- [x] 探测 030 v3 识别出长文本详情测试夹具不足；
- [x] 探测 030 v4 修正详情夹具并在 Android 14 真机确认 `ok=true`；
- [ ] 人工完成侧滑返回、三键返回和底部上拉最近任务复核；
- [ ] 真机确认关闭后不存在透明遮罩、遮暗层或不可触摸区域。

自动验证结论：模块集 `20260722.24` 已通过探测 030 v4。详情、编辑、筛选、首页返回链全部成功；四次返回均被处理；重复返回计数为 0；首页退出和合成最近任务退出后系统返回注册项均清零；后台实例继续运行；隔离运行锁正常释放。

当前状态：自动探测已完成，阶段 2D 仅剩真实系统手势、三键导航、最近任务和后台监听人工复核。

## 阶段 3：管理能力

### 阶段 3A：删除与撤销

- [x] 卡片删除按钮
- [x] SQLite 软删除
- [x] 最近一次删除撤销
- [x] 删除与恢复 EventBus 事件
- [x] 删除与撤销真机验证 014

当前状态：阶段 3A 已完成。

### 阶段 3B：搜索与过滤

- [x] Repository 参数化关键词搜索
- [x] 来源、类型、敏感、置顶与标签过滤
- [x] 多条件 AND 组合过滤
- [x] 活动过滤结果同步到 List
- [x] 搜索与组合过滤真机验证 015
- [x] 搜索输入框与选择 UI
- [x] 筛选界面真机验证 016
- [x] 中文关键词链路探测 024

当前状态：阶段 3B 已完成。

### 阶段 3C：编辑、排序与标签

- [x] 新增记录
- [x] 编辑正文
- [x] 项目置顶
- [x] 自定义标签 CRUD 与绑定
- [x] 标签过滤
- [x] 长文本与敏感详情
- [x] 分组手柄拖动排序
- [x] 新增、编辑与置顶探测 017
- [x] 标签探测 018 v2
- [x] 长文本详情探测 019
- [x] 排序事务与合成触控探测 020
- [x] 真实手指排序探测 021

当前状态：阶段 3C 已完成。

### 阶段 3D：统一视觉与自适应布局审查

- [x] 自动布局与安全区探测 022 v2
- [x] 第一轮多场景截图审查 023
- [x] Filter、Editor、Tag Editor 不透明模态与动态高度 025
- [x] Filter、Editor、Tag Editor 截图复核 026
- [x] 主列表元数据与操作区拆行
- [x] 长文本详情不透明模态与单一关闭入口
- [x] 主列表与详情功能回归 027
- [ ] 主列表与详情截图复核 028
- [ ] 平板与多窗口视觉验证

当前状态：模块集 `20260722.21` 已通过 027，028 的视觉结论保持有效；当前功能模块集已推进到 `20260722.24`。

## 当前模块集

```text
moduleSetVersion=20260722.24
entryVersion=4
databaseSchemaVersion=2
sourceRef=agent/initialize-project-skeleton
```

## 下一阶段

### 2D 系统手势与 UI 退出人工验证

自动探测 030 v4 已通过。剩余发布阻断项：

1. 启动正式 ClipHub `.24`；
2. 人工验证首页侧滑返回后全部 UI 消失；
3. 分别验证详情、编辑、筛选和标签页侧滑返回首页；
4. 验证首页三键返回与侧滑返回一致；
5. 验证底部上拉进入最近任务后全部 UI 消失；
6. 返回原应用，确认没有透明遮罩或不可触摸区域；
7. 在 UI 关闭后从其他应用复制新文本，确认仍可入库；
8. 再次打开 ClipHub，确认从首页开始。

### 3D2 平板与多窗口适配

- 平板宽屏尺寸；
- 横屏；
- 多窗口和分屏；
- 尺寸变化后的窗口夹紧；
- 次级窗口最大宽度。

### 3E 入口版本 5

- 正式实例运行中自动协调停止；
- 同步后自动启动新模块集；
- 更新失败时保持旧实例可恢复；
- 减少手工停止步骤。

### 发布前收口

1. 完成阶段 2D 系统手势专项真机验证；
2. 清理探测期临时说明和无效代码；
3. 更新 README 与架构文档；
4. 将 `ClipHub.js` 默认 ref 改为 `main`；
5. 将 manifest `sourceRef` 改为 `main`；
6. 执行完整 ES5 校验；
7. 重新执行发布前核心回归；
8. PR 保持 Draft，直到明确批准合并；
9. 最终采用 squash merge，并校验预期 head SHA。
