# ClipHub 开发计划

## 阶段 0：项目骨架

目标：建立能够持续迭代的独立项目。

- [x] 创建 GitHub 仓库 `7015725/ClipHub`
- [x] 初始化 `main`
- [x] 添加 README 和设计文档
- [ ] 确定许可证
- [x] 建立唯一 ShortX 入口 `ClipHub.js`
- [x] 建立模块加载器
- [x] 建立远程 `module-manifest.json`
- [x] 入口自动下载 15 个模块
- [x] Git blob SHA 完整性校验
- [x] 临时目录下载、旧模块备份和失败恢复
- [x] 完整本地缓存的离线回退
- [x] 建立日志目录和数据库目录
- [x] 增加重复启动保护
- [x] 增加 Rhino ES5 静态检查
- [x] 完成运行目录、作用域和文件锁真机探测
- [x] 完成入口首次下载与模块 SHA 真机验证
- [x] 完成真实模块停止、清理和二次启动验证

当前状态：阶段 0 已完成真机验证。

## 阶段 1：剪贴板数据链路

### 阶段 1A：SQLite 与 Repository 基础层

- [x] SQLite schema v1
- [x] 数据库迁移框架
- [x] 显式事务封装
- [x] 参数绑定执行接口
- [x] Cursor 查询与关闭边界
- [x] 剪贴板记录基础 CRUD
- [x] 软删除与恢复
- [x] 标签创建与记录关联基础接口
- [x] schema v1 → v2 迁移
- [x] `is_sensitive` 字段与索引

当前状态：阶段 1A 已完成。

### 阶段 1B：ClipboardManager 链路

- [x] ClipboardManager 监听
- [x] ClipData 文本读取
- [x] 主动写入接口
- [x] 自身写入回环抑制
- [x] 短时间重复合并
- [x] 同一 clip 重复回调抑制
- [x] 来源包名、应用名、UID 与置信度入库
- [x] 忽略应用运行期策略
- [x] 敏感 ClipData 默认跳过与允许保存策略
- [x] 连续复制压力与边界验证

当前状态：005 v2、006 v2、007 和 008 已全部通过。阶段 1B 已完成。

### 阶段 1C：设置持久化与历史保留

- [x] 复用 SQLite `settings` 表
- [x] 设置值 JSON 序列化和反序列化
- [x] 类型、范围与枚举校验
- [x] 历史数量裁剪
- [x] 保存天数清理
- [x] 置顶记录保护
- [x] 设置与清理真机验证 009

当前状态：阶段 1C 已完成。

## 阶段 2：基础悬浮窗

### 阶段 2A：WindowManager 生命周期

- [x] `TYPE_APPLICATION_OVERLAY`
- [x] add/update/remove 主线程同步
- [x] 标题栏拖动
- [x] WindowMetrics 与系统栏安全区域
- [x] 四边位置夹紧
- [x] 折叠、展开和固定状态
- [x] 亮暗色基础样式
- [x] 010 v2 与 011 真机验证

当前状态：阶段 2A 已完成。

### 阶段 2B：位置与尺寸生命周期

- [x] 窗口位置 SQLite 持久化
- [x] 归一化位置比例恢复
- [x] 屏幕旋转和尺寸变化监听
- [x] 安全区域变化后重新夹紧
- [x] 竖屏到横屏真机验证 012
- [ ] 平板和多窗口尺寸变化验证

当前状态：手机竖横屏范围已完成。

### 阶段 2C：最小历史列表

- [x] 原生 ScrollView 卡片列表
- [x] 最近记录查询与展示
- [x] Clipboard EventBus 自动刷新
- [x] 单击复制
- [x] 空状态
- [x] 敏感记录正文遮罩
- [x] 最小列表真机验证 013

当前状态：功能链路已完成，视觉审查并入阶段 3D。

### 阶段 2D：系统手势与 UI 退出（必要功能）

本阶段属于首版发布阻断条件。

- [x] 接入 Android 系统返回回调；
- [x] Android 13 及以上兼容 `OnBackInvokedDispatcher`；
- [x] Android 14 支持预测性返回进度、取消和提交；
- [x] 增加三键返回键和 Escape 返回后备链；
- [x] 次级页面返回上一层；
- [x] 首页返回关闭全部 ClipHub UI；
- [x] 页面显示后统一注册系统返回分发；
- [x] 增加进入最近任务或切换后台后的 UI 隐藏判断；
- [x] 后台隐藏时关闭筛选、编辑、详情、标签和主列表窗口；
- [x] UI 关闭后继续保持剪贴板监听、SQLite、运行锁和控制入口；
- [x] 再次打开时从首页开始；
- [x] 编辑未保存内容不会因进入后台自动提交；
- [x] 所有退出入口复用同一套幂等 UI 清理链；
- [x] 系统回调采用全局签名去重，程序化返回按页面所有者去重；
- [x] 关闭链清理残留 WindowManager View 和返回回调引用；
- [x] 增加系统手势专项探测 030；
- [x] 探测 030 v2 暴露全局 180ms 去重误吞编辑页和首页返回；
- [x] 模块集 `20260722.24` 修正不同页面连续返回和残留回调清理；
- [x] 探测 030 v3 识别出长文本详情测试夹具不足；
- [x] 探测 030 v4 修正详情夹具并在 Android 14 真机确认 `ok=true`；
- [x] 人工完成首页、详情、编辑、筛选和标签页真实侧滑返回复核；
- [x] 人工确认重新打开从首页开始、后台复制仍可入库且无透明遮罩残留；
- [x] 探测 031 定位 ColorOS 最近任务期间 Overlay 焦点保持为 `true`；
- [x] 模块集 `20260722.25` 增加仅在 UI 可见期间运行的 Root Task 观察器；
- [x] 探测 032 在真实底部上拉最近任务中确认自动关闭、回调清零和后台实例保留；
- [x] 使用正式实例复核模块集 `.25` 的底部上拉最近任务关闭；
- [x] 切换三键导航并复核详情、编辑、筛选、标签、首页和最近任务行为。

最终验证结论：模块集 `20260722.25` 已通过探测 030 v4、探测 032 和正式实例人工复核。真实侧滑返回、三键返回、最近任务自动关闭、返回回调清理、重新打开首页、后台复制入库和透明遮罩检查均通过 Android 14 / ColorOS 真机验证。

当前状态：阶段 2D 已完成。

## 阶段 3：管理能力

### 阶段 3A：删除与撤销

- [x] 卡片删除按钮
- [x] SQLite 软删除
- [x] 最近一次删除撤销
- [x] 删除与恢复 EventBus 事件
- [x] 删除与撤销真机验证 014

当前状态：阶段 3A 已完成。

### 阶段 3B：搜索与过滤

- [x] Repository 参数化关键词搜索
- [x] 来源、类型、敏感、置顶与标签过滤
- [x] 多条件 AND 组合过滤
- [x] 活动过滤结果同步到 List
- [x] 搜索与组合过滤真机验证 015
- [x] 搜索输入框与选择 UI
- [x] 筛选界面真机验证 016
- [x] 中文关键词链路探测 024

当前状态：阶段 3B 已完成。

### 阶段 3C：编辑、排序与标签

- [x] 新增记录
- [x] 编辑正文
- [x] 项目置顶
- [x] 自定义标签 CRUD 与绑定
- [x] 标签过滤
- [x] 长文本与敏感详情
- [x] 分组手柄拖动排序
- [x] 新增、编辑与置顶探测 017
- [x] 标签探测 018 v2
- [x] 长文本详情探测 019
- [x] 排序事务与合成触控探测 020
- [x] 真实手指排序探测 021

当前状态：阶段 3C 已完成。

### 阶段 3D：新 UI 全量复刻与自适应

旧 UI 的早期视觉审查结果作为功能和尺寸参考保留，但不再作为最终发布视觉目标。阶段 3D2 改为依据用户提供的 5 张 Android 端参考图，对主要页面进行完整复刻。

已完成的旧 UI 基础验证：

- [x] 自动布局与安全区探测 022 v2
- [x] 第一轮多场景截图审查 023
- [x] Filter、Editor、Tag Editor 不透明模态与动态高度 025
- [x] Filter、Editor、Tag Editor 截图复核 026
- [x] 主列表与详情功能回归 027

#### 阶段 3D2：五页面新 UI 全量复刻

- [x] 确认参考图范围为首页、筛选、编辑、详情和标签管理五个页面；
- [x] 删除错误方向的响应式视觉探测 033；
- [x] 建立 `docs/阶段3D2新UI全量复刻方案.md`；
- [ ] 3D2-0 建立共享视觉令牌、组件和统一浮层框架；
- [ ] 3D2-1 复刻首页 / 列表页；
- [ ] 3D2-2 复刻搜索 / 筛选页；
- [ ] 3D2-3 复刻新增 / 编辑页；
- [ ] 3D2-4 复刻详情 / 识别 / 翻译页；
- [ ] 3D2-5 复刻标签 / 分类管理页；
- [ ] 3D2-6 完成五页面统一截图对照与功能回归；
- [ ] 完成窄屏、横屏、分屏与多窗口适配；
- [ ] 重新执行 027、030 v4、032 和后台复制核心回归。

当前状态：阶段 3D2 已重新定向，下一步从共享视觉基础和首页 / 列表页开始，不再运行探测 033。

## 当前模块集

```text
moduleSetVersion=20260722.25
entryVersion=4
databaseSchemaVersion=2
sourceRef=agent/initialize-project-skeleton
```

## 下一阶段

### 3D2-0 共享视觉基础

1. 在 `ch_07_theme.js` 建立参考图对应的紫色主题、文字层级、圆角、间距和按钮令牌；
2. 在 `ch_08_window.js` 建立统一浮层底板、拖动手柄、遮暗层和页面内容宿主；
3. 建立通用卡片、胶囊标签、图标按钮、主按钮和底部操作栏；
4. 保持现有 WindowManager 生命周期、系统返回和最近任务退出逻辑不变。

### 3D2-1 首页 / 列表页

1. 复刻标题栏、搜索行、筛选入口和新增入口；
2. 复刻条目数量、排序提示和管理入口；
3. 复刻来源图标、正文、时间、类型标签、自定义标签、星标和更多按钮；
4. 复刻选中状态和底部快捷操作栏；
5. 保留拖动排序、复制、删除、置顶、编辑和翻译功能；
6. 生成首页首版真机截图，与参考图页面 1 逐项对照。

首页视觉基线确认后，再依次进入筛选、编辑、详情和标签管理页面。

### 3E 入口版本 5

- 正式实例运行中自动协调停止；
- 同步后自动启动新模块集；
- 更新失败时保持旧实例可恢复；
- 减少手工停止步骤。

### 发布前收口

1. 完成阶段 3D2 五页面新 UI 全量复刻与自适应验证；
2. 完成阶段 3E 入口升级与回归；
3. 清理探测期临时说明和无效代码；
4. 更新 README 与架构文档；
5. 将 `ClipHub.js` 默认 ref 改为 `main`；
6. 将 manifest `sourceRef` 改为 `main`；
7. 执行完整 ES5 校验；
8. 重新执行发布前核心回归；
9. PR 保持 Draft，直到明确批准合并；
10. 最终采用 squash merge，并校验预期 head SHA。
