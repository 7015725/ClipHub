# ClipHub 开发计划

## 阶段 0：项目骨架

目标：建立能够持续迭代的独立项目。

- [x] 创建 GitHub 仓库 `7015725/ClipHub`
- [x] 初始化 `main`
- [x] 添加 README 和设计文档
- [ ] 确定许可证
- [x] 建立唯一 ShortX 入口 `ClipHub.js`
- [x] 建立模块加载器与远程 Manifest
- [x] 入口自动下载 15 个模块
- [x] Git blob SHA 完整性校验
- [x] 临时目录下载、旧模块备份和失败恢复
- [x] 完整本地缓存的离线回退
- [x] 建立日志目录、数据库目录和重复启动保护
- [x] 增加 Rhino ES5 静态检查
- [x] 完成运行目录、作用域、文件锁和模块同步真机验证

当前状态：阶段 0 已完成真机验证。

## 阶段 1：剪贴板数据链路

### 阶段 1A：SQLite 与 Repository

- [x] SQLite schema v1 与迁移框架
- [x] 显式事务、参数绑定和 Cursor 关闭边界
- [x] 剪贴板记录 CRUD、软删除与恢复
- [x] 标签创建与记录关联
- [x] schema v1 → v2
- [x] `is_sensitive` 字段与索引

### 阶段 1B：ClipboardManager

- [x] ClipboardManager 监听和 ClipData 文本读取
- [x] 主动写入与自身回环抑制
- [x] 短时间重复合并与同一 clip 重复回调抑制
- [x] 来源包名、应用名、UID 与置信度入库
- [x] 忽略应用和敏感内容策略
- [x] 连续复制压力与边界验证 005–008

### 阶段 1C：设置与保留策略

- [x] SQLite 设置持久化
- [x] JSON 序列化、类型、范围与枚举校验
- [x] 历史数量和保存天数清理
- [x] 置顶记录保护
- [x] 真机验证 009

当前状态：阶段 1 已完成。

## 阶段 2：基础悬浮窗

### 阶段 2A：WindowManager 生命周期

- [x] `TYPE_APPLICATION_OVERLAY`
- [x] add/update/remove 主线程同步
- [x] 标题栏拖动、折叠、展开和固定
- [x] WindowMetrics、系统栏安全区和四边夹紧
- [x] 亮暗色基础样式
- [x] 真机验证 010 v2 与 011

### 阶段 2B：位置与尺寸

- [x] 窗口位置 SQLite 持久化
- [x] 归一化位置比例恢复
- [x] 屏幕旋转和尺寸变化监听
- [x] 安全区域变化后重新夹紧
- [x] 手机竖屏到横屏真机验证 012
- [ ] 平板和多窗口尺寸变化最终验证

### 阶段 2C：最小历史列表

- [x] 原生 ScrollView 卡片列表
- [x] 最近记录查询、EventBus 刷新和单击复制
- [x] 空状态与敏感记录遮罩
- [x] 真机验证 013

### 阶段 2D：系统手势与 UI 退出

- [x] Android 13+ `OnBackInvokedDispatcher`
- [x] Android 14 预测性返回进度、取消和提交
- [x] 三键返回和 Escape 后备链
- [x] 次级页面返回上一层，首页返回关闭全部 UI
- [x] UI 关闭后保留监听、SQLite、运行锁和控制入口
- [x] 关闭链清理 WindowManager View 和返回回调
- [x] 探测 030 v4 验证返回层级和回调清理
- [x] 探测 031 定位 ColorOS 最近任务焦点异常
- [x] 模块集 `.25` 增加 Root Task 观察器
- [x] 探测 032 验证真实底部上拉最近任务自动关闭
- [x] 正式实例侧滑、三键、最近任务、重新打开和后台复制复核
- [x] 无透明遮罩和触摸区域残留

最终结论：阶段 2D 已通过 Android 14 / ColorOS 真机验证。

## 阶段 3：管理能力

### 阶段 3A：删除与撤销

- [x] 卡片删除、SQLite 软删除和最近一次撤销
- [x] 删除与恢复 EventBus
- [x] 真机验证 014

### 阶段 3B：搜索与过滤

- [x] 关键词、来源、类型、敏感、置顶和标签过滤
- [x] 多条件 AND 组合和活动筛选结果同步
- [x] 搜索 / 筛选界面基础功能
- [x] 真机验证 015、016、024

### 阶段 3C：编辑、排序与标签

- [x] 新增和编辑正文
- [x] 项目置顶
- [x] 自定义标签 CRUD、绑定与过滤
- [x] 长文本与敏感详情
- [x] 分组手柄拖动排序
- [x] 真机验证 017–021

## 阶段 3D：新 UI 全量复刻与自适应

旧 UI 的早期视觉结果只保留为功能和尺寸参考。最终发布视觉以用户提供的 5 张 Android 端参考图为目标。

参考页面：

1. 首页 / 列表页
2. 搜索 / 筛选页
3. 新增 / 编辑页
4. 详情 / 识别 / 翻译页
5. 标签 / 分类管理页

### 阶段 3D2：五页面新 UI 全量复刻

#### 3D2-0：共享视觉基础

- [x] 确认五张参考图和页面范围
- [x] 建立 `docs/阶段3D2新UI全量复刻方案.md`
- [x] 建立紫色亮暗主题、文字、圆角、间距和状态色令牌
- [x] 重建底部浮层框架、拖动手柄、遮暗背景和内容宿主

#### 3D2-1：首页 / 列表页

- [x] 完成首页第一轮代码复刻并发布模块集 `.26`
- [x] 探测 033 结构、生命周期、安全区和正式实例恢复检查通过
- [x] 审查第一轮未裁剪截图并定位面板、卡片、图标和选择语义问题
- [x] 完成第二轮尺寸、卡片、来源图标、选择模式和底栏校正
- [x] 发布模块集 `.27`，List 升级为 v11
- [x] 探测 034 自动检查和未裁剪截图通过
- [x] 确认首页 / 列表页第二轮视觉基线

首页结论：Android 14 / ColorOS 真机窗口为 `390 × 688dp`，6 个来源应用图标全部加载，管理选中未触发复制，正式实例停止与恢复、隔离数据库和清理链正常。标题图标和少量间距留到 3D2-6 统一收口。

#### 3D2-2：搜索 / 筛选页

- [x] 完成 Filter v6 / `reference_search_v1` 第一轮实现
- [x] 完成探测 035 并确认测试夹具误报
- [x] 保持 Repository 既有关键词匹配语义
- [x] 完成 Filter v7 / `reference_search_v2` 和探测 036
- [x] 搜索结果 5 条、组合筛选 1 条、返回层级和历史恢复正常
- [x] 完成 Filter v8 / `reference_search_v3`：单行胶囊、尾部省略和中文宽度修正
- [x] 探测 037 自动验证通过，但真机截图确认敏感内容末行仍超出默认视口
- [x] 定位原因：Footer 总占用增大且探测只检查配置值，没有测量实际内容与 ScrollView 视口
- [x] 完成 Filter v9 / `reference_search_v4` 实际布局适配
- [x] 仅高级筛选胶囊使用 4dp 紧凑垂直内边距，搜索历史胶囊保持原尺寸
- [x] Footer 恢复 48dp，移除额外 topMargin；内容底部 padding 调整为 6dp
- [x] 增加实际内容高度、视口高度、滚动状态和首屏完整容纳状态
- [x] 抽屉高度继续保持 540dp
- [x] Repository、排序窗口、搜索历史、组合筛选和 Navigation 语义保持不变
- [x] 发布模块集 `20260722.31`
- [x] 新增探测 038
- [x] 探测 038 自动验证和两张未裁剪截图通过
- [x] 确认搜索 / 筛选页最终视觉基线

当前边界：第四轮使用 Android 实际 View 测量判定首屏完整容纳。Repository、排序窗口和 Navigation 语义保持不变。

#### 3D2-3：新增 / 编辑页

- [x] 完成 Editor v5 / `reference_editor_v1` 第一轮实现
- [x] 新增 / 编辑模式切换到共享紫色底部浮层
- [x] 增加标题图标、说明、只读类型 / 来源、字符计数和双按钮底栏
- [x] 标签管理模式保持原实现，留到 3D2-5 处理
- [x] 新增记录仍固定写入 text / ClipHub 手动
- [x] 编辑记录仍只更新 content，不改变来源、类型、敏感和置顶字段
- [x] `openNew` / `openItem` 增加兼容的可选 `requestKeyboard`
- [x] Navigation 继续通过 `ClipHub Editor Panel` 注册返回
- [x] 发布模块集 `20260722.32`
- [x] 新增探测 039
- [x] 探测 039 自动验证、保存边界、取消和返回层级全部通过
- [x] 新增页与编辑页两张未裁剪截图完成第一轮视觉审查
- [x] Editor v6 / `reference_editor_v2` 增加输入法和实际布局状态测量
- [x] 测量键盘可见、键盘占用、可见区域、正文视口和固定底栏屏幕位置
- [x] 增加长文本滚动方向、光标末尾和选择区状态
- [x] 保持 `SOFT_INPUT_ADJUST_RESIZE` 和 Navigation v3 实现不变
- [x] 发布模块集 `20260722.33`
- [x] 新增探测 040
- [x] 探测 040：输入法实际显示，但 Overlay 未随 `ADJUST_RESIZE` 收缩，固定底栏被键盘覆盖
- [x] 确认 `OnGlobalLayoutListener` 和 `getWindowVisibleDisplayFrame()` 在该窗口上未产生有效测量
- [x] 完成 Editor v7 / `reference_editor_v3` 输入法主动避让
- [x] Android 11+ 优先读取 Root WindowInsets，并以 InputMethodManager 可见高度和可见 Frame 作为回退
- [x] 键盘显示时仅更新现有 Editor WindowManager 参数，将窗口贴合到输入法上方
- [x] 键盘隐藏后恢复原 590dp 底部浮层位置
- [x] 增加 90ms 生命周期内 IME 轮询、窗口更新计数和避让状态
- [x] 保持 Repository 保存语义、标签管理和 Navigation v3 不变
- [x] 发布模块集 `20260722.34`
- [x] 新增探测 041
- [x] 探测 041：输入法避让、长文本和首次返回均通过，第二次返回因 EditText 继续持有焦点而未关闭 Editor
- [x] 定位传统 Back 键未到达根容器 `OnKeyListener`，Navigation v3 关闭逻辑本身未触发
- [x] 完成 Editor v8 / `reference_editor_v4` IME 隐藏后的焦点交接
- [x] 首次返回隐藏键盘后清除正文焦点，并将焦点恢复给已注册 Navigation 的 Editor Root
- [x] 增加焦点释放、根焦点请求和根焦点确认状态
- [x] 保持 Navigation v3、Repository 保存语义和输入法窗口尺寸逻辑不变
- [x] 发布模块集 `20260722.35`
- [x] 新增探测 042
- [ ] 运行探测 042 并回传三张未裁剪截图与完整 JSON
- [ ] 确认新增 / 编辑页输入法与两层返回最终基线

当前边界：本轮只处理输入法隐藏后的 Editor 焦点交接，不修改 Navigation v3，不新增自定义返回层，不改变 WindowManager 避让尺寸或 Repository 保存语义。
#### 后续页面

- [x] 3D2-3 第一轮新增 / 编辑页
- [ ] 3D2-4 复刻详情 / 识别 / 翻译页
- [ ] 3D2-5 复刻标签 / 分类管理页
- [ ] 3D2-6 完成五页面截图对照和功能回归
- [ ] 完成窄屏、横屏、分屏与多窗口适配
- [ ] 重新执行 027、030 v4、032 和后台复制核心回归

底部“翻译”暂时进入详情页，完整识别和翻译能力在 3D2-4 实现。

## 当前模块集

```text
moduleSetVersion=20260722.35
entryVersion=4
databaseSchemaVersion=2
sourceRef=agent/initialize-project-skeleton
```

当前模块版本：

```text
Theme=2
Window=5
List=11
Editor=8
Filter=9
Settings=4
Translation=4
Navigation=3
EventBus=2
RecentsWatch=1
```
## 下一步

### 运行新增 / 编辑焦点交接与两层返回探测 042

1. 在 Termux 同步 `agent/initialize-project-skeleton`；
2. 运行 `ClipHub.js`，确认模块集为 `.35`；
3. 运行 `ClipHub 新增编辑焦点返回探测042`；
4. 场景 1 截取输入法显示且固定底栏位于键盘上方的短文本页；
5. 场景 2 截取长文本光标位于末尾的页面；
6. 场景 3 截取首次返回隐藏键盘后恢复底部浮层的页面；
7. 回传三张未裁剪截图和完整 JSON；
8. 检查首次返回后 `inputFocused=false`、Root 已取得焦点；
9. 检查第二次返回由 Navigation 处理并关闭 Editor；
10. 检查未保存内容不入库、正式实例恢复和清理链正常。
### 后续阶段 3E：入口版本 5

- 正式实例运行中自动协调停止
- 同步后自动启动新模块集
- 更新失败时保持旧实例可恢复
- 减少手工停止步骤

### 发布前收口

1. 完成五页面新 UI 与自适应验证；
2. 完成入口版本 5；
3. 清理探测期临时说明和无效代码；
4. 更新 README 与架构文档；
5. 将入口和 Manifest 默认 ref 改为 `main`；
6. 执行完整 ES5 与核心真机回归；
7. PR 保持 Draft，直到明确批准合并；
8. 最终采用 squash merge，并校验预期 head SHA。
