# 筛选模块正式化边界

- 基线：`20260724.26` / `agent/unify-window-geometry`
- 保留顶部紧凑布局、首页搜索、搜索历史、可逆 IME 避让、高级筛选条件、拖动与右下角缩放。
- 删除高级筛选内部搜索及其输入框、动作、公开测试入口和状态引用。
- 不修改 Repository 搜索语义、SQLite Schema、剪贴板监听、标签、翻译、复制、删除撤销、置顶或列表手势。
- 将 `ch_11_filter.js` 从远程下载、字符串补丁、`eval()` 和缓存回退加载器收敛为完整直接模块。
- PR #29 保持 Draft，不合并 `main`。
