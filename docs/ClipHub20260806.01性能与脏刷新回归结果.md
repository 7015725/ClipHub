# ClipHub 20260806.01 性能与脏刷新回归结果

## 测试环境

- Android 14
- ShortX / Rhino ES5
- 分支：`agent/optimize-cliphub-window-startup-v1-20260805`
- 入口版本：6
- 模块集版本：`20260806.01`
- 标准数据规模：20 条记录

## 暖启动性能回归

探针：`cliphub_startup_performance_probe_002`

结果：

- `ok=true`
- `preflightPassed=true`
- `cyclesCompleted=20`
- 窗口缓存复用 20/20
- 内容就绪 20/20
- 首次绘制指标 20/20
- 超时 0
- 缺失性能指标 0

### 性能汇总

| 指标 | 最小值 | 中位数 | P95 | 最大值 | 平均值 |
|---|---:|---:|---:|---:|---:|
| 窗口挂载 | 18.348 ms | 26.690 ms | 34.615 ms | 34.936 ms | 27.255 ms |
| 首次绘制 | 34.869 ms | 51.454 ms | 62.348 ms | 64.199 ms | 52.026 ms |
| 首批内容 | 18.420 ms | 26.781 ms | 34.711 ms | 35.095 ms | 27.370 ms |
| 完整内容 | 18.420 ms | 26.781 ms | 34.711 ms | 35.095 ms | 27.370 ms |

全部 20 轮均为：

- `windowCacheReused=true`
- `contentReady=true`
- `renderedCount=20`
- `renderBatchCount=0`

无数据变化时直接复用已渲染卡片，没有重复分批构建。

### 与 20260805.03 对比

- 窗口挂载中位数：28.668 ms → 26.690 ms。
- 首次绘制中位数：53.988 ms → 51.454 ms。
- 窗口挂载和首次绘制中位数均未退化。
- 首次绘制 P95 为 62.348 ms，仍远低于既定目标。

## 隐藏期间脏数据刷新回归

探针：`cliphub_dirty_refresh_probe_004`

结果：

- `ok=true`
- `preflightPassed=true`
- `cachePrepared=true`
- `clipboardChanged=true`
- `rapidPairsCompleted=10`
- `rapidRacePassed=true`
- `restoredInitialVisibility=true`
- `failureStage=null`
- `error=null`

### 缓存确认

无数据变化时：

- `windowCacheReused=true`
- `contentReady=true`
- 窗口挂载 21.667 ms
- 首次绘制 48.208 ms
- 首批和完整内容 21.831 ms
- `renderBatchCount=0`

### 脏数据刷新

隐藏期间复制 149 字符的新文本后：

- `windowCacheReused=true`
- `contentReady=true`
- 窗口挂载 26.168 ms
- 首次绘制 43.033 ms
- 首批内容 185.833 ms
- 完整 20 条内容 563.755 ms
- `renderBatchCount=5`

5 批渲染符合 `6 + 4 + 4 + 4 + 2` 的设计。

与上一轮 20260805.03 脏刷新相比：

- 首批内容：243.991 ms → 185.833 ms。
- 完整内容：649.248 ms → 563.755 ms。
- 脏刷新首批和完整渲染均进一步缩短。

### 快速显示与隐藏

连续 10 组均满足：

- 显示返回 `shown`
- 隐藏返回 `hidden`
- `hiddenAfterPair=true`

此前探针 004 暴露的第 2–10 组窗口残留没有再次出现。

## 结论

`20260806.01` 已通过以下自动回归：

1. 20 轮暖启动和缓存复用。
2. 隐藏期间剪贴板变化。
3. 20 条数据的 5 批刷新。
4. 10 组快速显示与隐藏。
5. 最终缓存复用和状态恢复。

下一阶段为系统交互回归：系统侧滑返回、外部点击关闭、Home/最近任务、拖动缩放、共享几何与 IME 避让。
