# ClipHub 搜索与组合过滤探测 015 结果

## 结论

探测 015 全部通过，模块集为 `20260721.14`。

```text
ok=true
durationMs=896
seededActiveCount=6
seededDeletedCount=1
keywordCount=4
sourceCount=3
contentTypeCount=2
combinedCount=1
multiCombinedCount=3
sourceLabelSearchCount=2
literalWildcardCount=1
injectionSafe=true
deletedExcluded=true
pinnedOnlyCount=1
normalizedCriteria=true
filteredEmptyState=true
filterEventNoContent=true
eventFilterReapplied=true
eventApplyCount=1
renderThreadName=main
resetRestoredAll=true
filterResetAfterRestart=true
renderedAllAfterRestart=true
firstDatabaseClosed=true
secondDatabaseClosed=true
formalRestart.ok=true
cleanup=true
error=null
```

## 已验证边界

- 关键词可匹配正文、来源名称和来源包名；
- 来源应用和内容类型支持单选、多选及 AND 组合；
- `%`、`_` 和反斜杠按 LIKE 字面量处理；
- SQL 注入样式关键词不会改变查询结构；
- 软删除记录不会进入普通筛选结果；
- 活动筛选会在剪贴板事件后重新应用；
- `filter_changed` 事件不携带正文字段；
- 筛选条件不持久化，重启后恢复全部记录；
- Window、SQLite、文件锁和隔离目录均完整清理。

## 后续

查询引擎验证完成，下一步为搜索输入框、来源应用选择器和内容类型选择器的真实 UI 探测 016。
