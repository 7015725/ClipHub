# ClipHub WindowManager 探测 010 v2 结果

## 结论

探测通过：

```text
ok=true
probeVersion=2
moduleSetVersion=20260721.10
durationMs=532
error=null
```

## 正式实例控制

```text
formalControl.ok=true
formalControl.initiallyRunning=true
formalControl.ackReceived=true
formalControl.ack.threadName=main
formalControl.lockReleased=true
formalControl.endpointRemoved=true
```

## WindowManager 生命周期

```text
windowType=2038
addThreadName=main
removeThreadName=main
attachedToWindow=true
firstAttachWaitMs=26
reopenSucceeded=true
reopenAttachWaitMs=26
reopenOpenCount=2
```

首次 `open()` 返回时 `state.attachedToWindow=false`，但在 26 ms 后真实 View 已完成 attach。这验证了 010 v1 的失败属于首帧前读取状态，而不是 `addView()` 失败。

## 功能验证

以下项目全部通过：

- 重复 `open()` 复用同一窗口；
- 标题栏拖动监听器已安装；
- 左上安全区夹紧；
- 右下安全区夹紧；
- `moveBy()` 更新路径；
- 折叠与展开；
- 固定状态；
- 状态文本更新；
- `removeViewImmediate()` 后完全 detached；
- 重复关闭幂等；
- 关闭后第二次打开；
- App 停止后数据库关闭；
- 正式实例恢复；
- 隔离目录清理。

## 设备数据

```text
safeBounds.left=0
safeBounds.top=140
safeBounds.right=1264
safeBounds.bottom=2780
firstWindow.width=984
firstWindow.height=554
collapsedHeight=178
```

## 下一步

阶段 2A 自动生命周期验证完成。下一探测 011 只验证真实手指拖动：

- 使用真实标题栏触摸事件；
- `dragMoveCount>0`；
- 最终坐标发生变化；
- 更新线程为 `main`；
- 最终位置仍在安全区域内。
