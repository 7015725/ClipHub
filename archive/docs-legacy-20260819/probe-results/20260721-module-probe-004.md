# ClipHub 真实模块探测 004 结果

## 环境

```text
日期：2026-07-21
Android：14 / SDK 34
进程：system_server
pid：3284
uid：1000
线程：DefaultDispatcher-worker-7
ShortX 根目录：/data/system/shortx_OYdazjKnxMzhQykL
模块集：20260721.3
```

## 结果

```text
ok = true
durationMs = 34
localManifestPresent = true
installedModuleCount = 15
moduleHashesValid = true
schemaVersion = 1
isolatedDatabase = true
insertedId = 1
contentRoundTrip = true
hashLength = 64
updateApplied = true
tagLinkCount = 1
rollbackObserved = true
deleteCounts.active = 0
deleteCounts.total = 1
restoreCount = 1
detachCount = 0
stopped = true
databaseClosed = true
secondStartCount = 1
secondStop = true
cleanup = true
error = null
```

## 验证结论

真实入口下载后的 15 个模块通过以下验证：

- 本地 manifest 存在；
- 所有模块 Git blob SHA 匹配；
- schema v1 正常；
- Repository 插入、读取、更新正常；
- 原始文本与 Emoji 往返正常；
- SHA-256 摘要长度和内容更新正常；
- 标签创建、关联和解除正常；
- 显式事务回滚正常；
- 软删除和恢复正常；
- 停止后数据库关闭、锁释放；
- 相同隔离目录可以二次启动并读取原数据；
- 探测目录成功清理。

## 阶段判断

阶段 1A「SQLite 与 Repository 基础层」通过，可以进入阶段 1B「ClipboardManager 链路」。
