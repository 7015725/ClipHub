name: Apply ClipHub editor IME avoidance

on:
  pull_request:
    branches:
      - agent/initialize-project-skeleton
    paths:
      - .github/cliphub-editor-ime-avoidance-trigger.txt

permissions:
  contents: write

concurrency:
  group: cliphub-editor-ime-avoidance
  cancel-in-progress: false

jobs:
  apply:
    if: github.repository == '7015725/ClipHub'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/initialize-project-skeleton
          fetch-depth: 0
      - name: Apply editor IME avoidance
        shell: bash
        run: bash .github/cliphub-editor-ime-avoidance/apply.sh
