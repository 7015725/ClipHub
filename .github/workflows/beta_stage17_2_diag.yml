name: Beta Stage17.2 Build
on:
  push:
    branches: [beta-pagination-stage10-20260808]
    paths:
      - .github/stage17_2_diag_trigger.txt
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Build packed Filter67 staged retarget
        shell: bash
        run: |
          set -euo pipefail
          python stage17_2_build_runner.py
