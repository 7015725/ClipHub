name: Apply ClipHub sole filter-root migration

on:
  pull_request:
    branches:
      - agent/initialize-project-skeleton
    paths:
      - .github/cliphub-filter-root-trigger.txt

permissions:
  contents: write
  issues: write
  pull-requests: write

concurrency:
  group: cliphub-filter-root
  cancel-in-progress: false

jobs:
  apply:
    if: github.repository == '7015725/ClipHub'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/initialize-project-skeleton
          fetch-depth: 0
      - name: Apply sole filter-root migration
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set +e
          bash -x .github/cliphub-filter-root/apply.sh > /tmp/cliphub_filter_root.log 2>&1
          code=$?
          if [ "$code" -ne 0 ]; then
            {
              echo '```text'
              tail -n 180 /tmp/cliphub_filter_root.log
              echo '```'
            } > /tmp/cliphub_filter_root_comment.md
            gh api repos/${{ github.repository }}/issues/11/comments --method POST --field body=@/tmp/cliphub_filter_root_comment.md || true
          fi
          exit "$code"
