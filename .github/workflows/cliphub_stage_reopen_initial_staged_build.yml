name: ClipHub Reopen Initial Staged Build

on:
  push:
    branches:
      - beta-regex-settings-tabs-20260814
    paths:
      - .github/cliphub_stage_reopen_initial_staged_build.py
      - .github/workflows/cliphub_stage_reopen_initial_staged_build.yml

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: beta-regex-settings-tabs-20260814
          fetch-depth: 0
      - name: Patch packed Filter75 safely
        shell: bash
        run: |
          set -euo pipefail
          python .github/cliphub_stage_reopen_initial_staged_build.py
