name: Beta Stage21 Build
on:
  push:
    branches: [beta-pagination-stage10-20260808]
    paths:
      - .github/stage21_build_trigger.txt
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Build Filter71
        shell: bash
        run: |
          set -euo pipefail
          python stage21_build.py
